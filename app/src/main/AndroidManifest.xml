<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.csumb.project1_cst438">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Grading App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Model.ACT_Edit_Course"></activity>
        <activity android:name=".Model.ACT_Detailed_Course_Editable" />
        <activity android:name=".tests" />
        <activity android:name=".Model.ACT_Initial_Course_Display" />
        <activity android:name=".Model.ACT_Add_Course" />
        <activity android:name=".CategoryActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
    </application>

</manifest>